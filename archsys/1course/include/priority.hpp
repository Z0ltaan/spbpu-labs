#ifndef PRIORITY_HPP
#define PRIORITY_HPP

#define EMPTY -1

#include <cstddef>

namespace course
{
  using priority_t = short int;
  using deviceid_t = priority_t;
  using requestid_t = size_t;
} // namespace course

#endif
