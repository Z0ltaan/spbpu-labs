program.txt and interactive_mode.txt must be placed in configs directory near the binary.

interactive_mode.txt represents a single boolean value (on/off)

program.txt structure:

producer_count
device(consumer)_count
buffer_size
simulation_end(floating point value)
puasson_lambda
uniform_distribution_lower_bound
uniform_distribution_upper_bound
